# OpenUkulele Issue 台账

最后更新：2026-09-14

## 状态说明

- 当前分支：`feat/ukulele-tab-cli`
- 基线验证：`.venv\\Scripts\\python -m pytest -q`，107 项通过。
- GitHub 远程：当前没有已登记的 issue。
- 本台账只记录经本地检查确认的事项；每项关闭前必须满足其验收标准。

| ID | 优先级 | 状态 | 问题与证据 | 建议处理 | 验收标准 |
| --- | --- | --- | --- | --- | --- |
| DOC-001 | P0 | 待处理 | README 开发段写“105 项单元/集成/CLI 测试”，本地实际运行结果为 107 项通过。 | 将 README 中的测试数改为 107，或改成不易过时的“运行 pytest 查看当前测试数”。 | README 与 `pytest -q` 输出一致。 |
| LYR-001 | P0 | 设计中 | `src/uketab/lyrics.py`、`lyrics-asr.lrc` 未提交；歌词功能尚未接入 CLI、渲染器或测试。 | 以独立功能分支实现 `--lyrics`，先完成 LRC/纯文本解析、按旋律对齐、渲染和报告，再提交。 | 命令能读取有效歌词；无歌词时输出与当前完全一致；新增单元和集成测试通过。 |
| LYR-002 | P0 | 设计中 | 当前 LRC 最后一行只分配到“最后旋律音 + 1 beat”，尾句容易被截断；相邻行的容忍窗口可能竞争同一旋律音。 | 采用不重叠的 LRC 行窗口和明确的尾部窗口规则；把对齐缺失写入报告。 | 覆盖尾句、相邻时间戳、重复时间戳、歌词多于/少于旋律音的自动化测试。 |
| QA-001 | P1 | 待处理 | 自动化测试主要使用 MIDI fixture 与 mock 音频；尚无真实手机录音回归数据。 | 建立不提交 Git 的本地 `samples/private/` 回归清单，记录输入格式、tempo、主观可弹性和已知转写缺陷。 | 至少 3 个有权使用的 30–60 秒录音，完成手工试听、导出打开和实际演奏检查。 |
| ARR-001 | P1 | 待处理 | 高 G re-entrant 已正确建模，但当前成本函数未专门奖励 campanella 的跨弦延音。 | 在稳定的基础编配后，增加可配置的跨弦延音奖励，并用样例对比验证。 | 新样例保持校验通过，且目标音阶片段的相邻同音不被无必要地截断。 |
| PRIV-001 | P1 | 待处理 | 图片/PDF 默认水印包含个人邮箱。 | 默认关闭水印；用户显式传 `--watermark` 时再写入。 | 未传该参数的 PNG/PDF 不包含个人信息；显式参数仍可生效。 |
| CI-001 | P2 | 待处理 | 当前没有 CI，依赖升级或不同 Python 版本的回归无法自动发现。 | 新建 GitHub Actions，在 Python 3.11、3.12、3.13 上安装 `.[dev]` 并运行 pytest。 | 拉取请求和推送均展示测试状态；三个版本均绿。 |

## 处理顺序

1. `DOC-001`：随下一次文档提交修正。
2. `LYR-001`、`LYR-002`：作为一个完整的歌词功能迭代处理。
3. `QA-001`：等用户提供合法手机录音后建立本地回归基线。
4. `PRIV-001` 与 `CI-001`：在歌词迭代后处理。
5. `ARR-001`：在已有真实录音的听感反馈后再调参，避免过早优化。
