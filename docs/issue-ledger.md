# OpenUkulele Issue 台账

最后更新：2026-09-14

## 状态说明

- 当前分支：`fix/lyrics-hardening`
- 基线验证：`.venv\\Scripts\\python -m pytest --collect-only -o addopts=''` 收集到 120 项；全量 pytest 通过。
- GitHub 远程：当前没有已登记的 issue。
- 本台账只记录经本地检查确认的事项；每项关闭前必须满足其验收标准。

| ID | 优先级 | 状态 | 问题与证据 | 建议处理 | 验收标准 |
| --- | --- | --- | --- | --- | --- |
| DOC-001 | P0 | 已解决（2026-09-14） | README 开发段硬编码“105 项单元/集成/CLI 测试”。 | 改为不随测试数量漂移的命令说明。 | 已验证全量 pytest 通过。 |
| LYR-001 | P0 | 已解决（2026-09-14） | 歌词功能曾处于工作区草稿状态。 | 已通过 `--lyrics` 接入 LRC/纯文本解析、旋律对齐、ASCII/图片渲染与集成测试。 | 无歌词输出保持原样；歌词 CLI 测试通过。 |
| LYR-002 | P0 | 已解决（2026-09-14） | 根因是相邻 LRC 行的 `±1 beat` 容忍窗口重叠，前一行可抢占后一行 onset；“末行截断”未能复现，已从问题描述移除。 | 默认采用不重叠的半开时间窗口；容忍值保留为显式调用方选项。 | 相邻时间戳的回归测试通过，第二行映射到自身起始音。 |
| QA-001 | P1 | 待处理 | 自动化测试主要使用 MIDI fixture 与 mock 音频；尚无真实手机录音回归数据。 | 建立不提交 Git 的本地 `samples/private/` 回归清单，记录输入格式、tempo、主观可弹性和已知转写缺陷。 | 至少 3 个有权使用的 30–60 秒录音，完成手工试听、导出打开和实际演奏检查。 |
| ARR-001 | P1 | 待处理 | 高 G re-entrant 已正确建模，但当前成本函数未专门奖励 campanella 的跨弦延音。 | 在稳定的基础编配后，增加可配置的跨弦延音奖励，并用样例对比验证。 | 新样例保持校验通过，且目标音阶片段的相邻同音不被无必要地截断。 |
| PRIV-001 | P1 | 已解决（2026-09-14） | 图片/PDF 默认水印包含个人邮箱。 | 默认关闭水印；用户显式传 `--watermark` 时再写入。 | CLI 回归测试确认未传参数时两份输出均接收空水印。 |
| CI-001 | P2 | 待处理 | 当前没有 CI，依赖升级或不同 Python 版本的回归无法自动发现。 | 新建 GitHub Actions，在 Python 3.11、3.12、3.13 上安装 `.[dev]` 并运行 pytest。 | 拉取请求和推送均展示测试状态；三个版本均绿。 |

## 处理顺序

1. `QA-001`：等用户提供合法手机录音后建立本地回归基线。
2. `CI-001`：在 Python 3.11、3.12、3.13 上建立自动测试。
3. `ARR-001`：在已有真实录音的听感反馈后再调参，避免过早优化。
